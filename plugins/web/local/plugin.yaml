name: web-local
version: 1.0.0
description: "Local web content extraction via trafilatura, readability-lxml, BeautifulSoup, and PyMuPDF — no API key required."
author: NousResearch
kind: backend
provides_web_providers:
  - local